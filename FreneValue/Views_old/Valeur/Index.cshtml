@model IEnumerable<FreneValue.Models.VAL_DOM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Gestion des codes valeurs</h2>

<div align="right"> @Html.ActionLink("Ajouter une valeur", "Create") </div>

@*@using (Html.BeginForm("Index", "Valeur", FormMethod.Get))
{
    @Html.DropDownList("codedom", new SelectList(ViewBag.codedom))
    <input type="submit" value="Filtrer ici" />
}

<hr/>
<br />*@ 

@using (Ajax.BeginForm("Rechercher", "Valeur", new AjaxOptions{
                                               HttpMethod = "GET",
                                               InsertionMode = InsertionMode.Replace,
                                               UpdateTargetId = "RstRecherche"

                     }))
{
    <input type="text" name="q" data-autocomplete="@Url.Action("RechercherRapid", "Valeur")" />
    <input type="submit" value="Rechercher"  />
}


  

   <table class="table table-bordered table-striped" id="RstRecherche">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.COD_DOM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.COD_VAL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VAL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ACTIF)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.UTIL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DT_CRETN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DT_MODF)
        </th>*@
        <th>
            Action
        </th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.COD_DOM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.COD_VAL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VAL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DESCRIP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ACTIF)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.UTIL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DT_CRETN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DT_MODF)
            </td>*@

            <td>
                <div class="pull-right">

                    @Html.ActionLink(" ", "Edit",  new { id = item.ID }, new { @class = "glyphicon glyphicon-pencil", title = "Modifier" })
                    &nbsp;
                    &nbsp;
                    @Html.ActionLink(" ", "Delete",  new { id = item.ID }, new { @class = "glyphicon glyphicon-trash", title = "Supprimer" })
                </div>
            </td>

            @*<td>
                @Html.ActionLink("Modifier", "Edit", new { id=item.ID }) |
                @@Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Supprimer", "Delete", new { id=item.ID })
            </td>*@
        </tr>
        }

</table>
