@using FreneValue.Helpers
@model FreneValue.Models.loclsn

@{
    ViewBag.Title = "PreCreate";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*@using (Html.BeginForm())
    {
        <td>
            @Html.DropDownListFor(x => x.SelectedValue, ViewBag.typloc.Items as List<SelectListItem>, "Choisir type localisation", new { @class = "form-control" })
        </td>
        <td>
            No Description
        </td>
        <td>
            @Html.ActionLink("Add", "AddSoftware", new { pDeviceId = Model.Id, pSoftwareId = Model.SelectedValue })
        </td>
    }*@

<div>
    <h2>Création d'une localisation </h2>

    @using (Html.BeginForm())
    {
        <div class="form-group">
            @*@Html.LabelFor(model => model.typ_loc, htmlAttributes: new { @class = "control-label col-md-4" })*@
            <div class="col-md-8">
                Quel type de localisation voulez-vous créer?
                @*@Html.DropDownListFor(model => model.typ_loc, ViewBag.typloc.Items as List<SelectListItem>, new { @class = "form-control" })
                @Html.EditorFor(model => model.typ_loc, new { htmlAttributes = new { @class = "form-control" } })*@
                @Html.DropDownList("typ_loc", ViewBag.typloc.Items as List<SelectListItem>, "-- select an option --", new { id = "typ_loc", @class = "form-control" })
                
                @*@Html.DropDownList("typ_loc", ViewBag.typloc.Items as List<SelectListItem>, new { @class = "form-control" })*@
            </div>
        </div>

        <div>
            <p>
              @Html.NoEncodeActionLink("Continuer", "Add", "Create1", "Localisation", routeValues: new {  }, htmlAttributes: new { data_modal = "", @class = "btn btn-primary " })
            </p>
        </div>

        <div>
            @Ajax.ActionLink("Continuer", "Create1", "Localisation", new { typ_loc = Model.typ_loc }, new AjaxOptions
               {
                   UpdateTargetId = "creloc",
                   InsertionMode = InsertionMode.Replace,
                   HttpMethod = "GET"
               }, new { @class = "btn btn-primary " })
        </div>
    }

    <div id="creloc">
    </div>

</div>
